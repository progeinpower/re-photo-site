---
const SITE_TITLE = "Ali Shibly | Real Estate Photography";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width,initial-scale=1" />
		<title>{SITE_TITLE}</title>
		
		<style>
			:root { --accent: #2563eb; --dark: #1e293b; --light: #f8fafc; }
			body { margin: 0; font-family: system-ui, sans-serif; background: var(--light); color: var(--dark); }
			
			/* HERO */
			.hero {
				background: var(--dark);
				color: white;
				padding: 8rem 1rem;
				text-align: center;
			}
			.hero h1 { font-size: 3.5rem; margin: 0; line-height: 1.1; }
			.hero p { font-size: 1.25rem; color: #cbd5e1; max-width: 600px; margin: 1rem auto; }
			.btn { 
				display: inline-block; background: var(--accent); color: white; 
				padding: 1rem 2rem; border-radius: 50px; text-decoration: none; font-weight: bold; margin-top: 1rem;
			}

			/* GRID */
			.container { max-width: 1200px; margin: 0 auto; padding: 4rem 1rem; }
			h2 { text-align: center; font-size: 2.5rem; margin-bottom: 3rem; }
			.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; }
			.card { background: white; padding: 2rem; border-radius: 12px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); }
		</style>
	</head>
	<body>
		<section class="hero">
			<h1>Ali Shibly<br>Real Estate Photography</h1>
			<p>Premium HDR photography, drone shots, and virtual tours to sell listings faster.</p>
			<a href="mailto:your-email@example.com" class="btn">Book a Shoot</a>
		</section>

		<div class="container">
			<h2>Services</h2>
			<div class="grid">
				<div class="card">
					<h3>📸 Interior & Exterior</h3>
					<p>Wide-angle HDR photography that captures the true space and light of every room.</p>
				</div>
				<div class="card">
					<h3>🚁 Drone Aerials</h3>
					<p>Showcase the lot, location, and surrounding neighborhood from the sky.</p>
				</div>
				<div class="card">
					<h3>🏠 Virtual Tours</h3>
					<p>Immersive 3D walkthroughs allowing buyers to visit from anywhere.</p>
				</div>
			</div>
		</div>
	</body>
</html>