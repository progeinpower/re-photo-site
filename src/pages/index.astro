---
const SITE_TITLE = "Ali Shibly | Real Estate Photography";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width,initial-scale=1" />
		<title>{SITE_TITLE}</title>
		
		<style>
			/* --- GLOBAL STYLES --- */
			:root {
				--accent: #2563eb; /* Blue */
				--accent-dark: #1e40af;
				--dark: #1e293b;   /* Dark slate */
				--light: #f8fafc;  /* Light gray bg */
				--white: #ffffff;
				--text-gray: #64748b;
			}
			body {
				margin: 0;
				font-family: system-ui, -apple-system, sans-serif;
				background: var(--light);
				color: var(--dark);
				line-height: 1.6;
			}
			h1, h2, h3 { margin-top: 0; line-height: 1.2; }
			h2 { font-size: 2.5rem; text-align: center; margin-bottom: 3rem; }
			.container { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; }
			.grid { display: grid; gap: 2rem; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }
			.card { background: var(--white); padding: 2rem; border-radius: 12px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05); border: 1px solid #e2e8f0; }
			.section-padding { padding: 5rem 0; }
			.btn { 
				display: inline-block; background: var(--accent); color: white; 
				padding: 0.8rem 1.5rem; border-radius: 50px; text-decoration: none; 
				font-weight: bold; transition: background 0.2s; border: none; cursor: pointer;
			}
			.btn:hover { background: var(--accent-dark); }

			/* --- HERO SECTION WITH FADED BACKGROUND --- */
			.hero {
				position: relative; /* Needed for the pseudo-element overlay */
				color: white;
				padding: 10rem 1.5rem;
				text-align: center;
				overflow: hidden; /* Ensures the image doesn't spill out */
				background-color: var(--dark); /* Fallback color */
			}
			/* This creates the faded image overlay */
			.hero::before {
				content: '';
				position: absolute;
				top: 0; left: 0; width: 100%; height: 100%;
				/* Placeholder Winnipeg Skyline Image - REPLACE THIS LATER */
				background-image: url('https://images.unsplash.com/photo-1575900752464-5347bc228b8e?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
				background-size: cover;
				background-position: center;
				opacity: 0.25; /* Controls the "fade" level (0.0 to 1.0) */
				z-index: 0; /* Places it behind the text */
			}
			/* Ensures text sits on top of the faded image */
			.hero-content { position: relative; z-index: 1; }
			.hero h1 { font-size: 3.5rem; font-weight: 800; margin-bottom: 1rem; }
			.hero p { font-size: 1.25rem; color: #cbd5e1; max-width: 600px; margin: 0 auto 2rem auto; }

			/* --- SERVICES SECTION --- */
			.services-icon { font-size: 2.5rem; margin-bottom: 1rem; display: block; }

			/* --- QUOTE TOOL STYLES --- */
			#quote-tool { background: var(--white); }
			/* Bundles */
			.bundle-card { 
				text-align: center; cursor: pointer; transition: all 0.2s; border: 2px solid transparent;
			}
			.bundle-card:hover { border-color: var(--accent); transform: translateY(-3px); }
			.bundle-card h3 { margin-bottom: 0.5rem; }
			.bundle-price { font-size: 1.5rem; font-weight: bold; color: var(--accent); display: block; margin: 1rem 0; }
			.bundle-features { list-style: none; padding: 0; margin: 0 0 1.5rem 0; color: var(--text-gray); font-size: 0.9rem; }
			.bundle-features li { margin-bottom: 0.5rem; }
			/* Pick & Choose */
			.ala-carte-list { display: grid; gap: 1rem; }
			.checkbox-label { 
				display: flex; align-items: center; justify-content: space-between;
				padding: 1rem; background: var(--light); border-radius: 8px; cursor: pointer;
			}
			.checkbox-label input { margin-right: 1rem; accent-color: var(--accent); transform: scale(1.2); }
			/* Total Price Box */
			.quote-total-box {
				background: var(--dark); color: white; padding: 2rem; border-radius: 12px;
				text-align: center; margin-top: 3rem;
			}
			.total-price-display { font-size: 3rem; font-weight: 800; line-height: 1; margin: 0.5rem 0 1.5rem 0; }
		</style>
	</head>
	<body>
		
		<section class="hero">
			<div class="hero-content">
				<h1>Ali Shibly<br>Real Estate Photography</h1>
				<p>Capturing Winnipeg's finest properties. Premium HDR photography, drone aerials, and immersive virtual tours to sell listings faster.</p>
				<a href="#quote-tool" class="btn">Get an Instant Quote</a>
			</div>
		</section>

		<section class="section-padding container">
			<h2>Our Premium Services</h2>
			<div class="grid">
				<div class="card">
					<span class="services-icon">📸</span>
					<h3>Interior & Exterior Photos</h3>
					<p>Stunning wide-angle HDR photos that showcase the true flow, space, and light of every room.</p>
				</div>
				<div class="card">
					<span class="services-icon">🚁</span>
					<h3>Aerial Drone Imagery</h3>
					<p>Highlight the property's location, lot size, and surrounding neighborhood from a unique perspective.</p>
				</div>
				<div class="card">
					<span class="services-icon">🏠</span>
					<h3>3D Virtual Tours</h3>
					<p>Create an immersive 24/7 open house experience, allowing buyers to walk through from anywhere.</p>
				</div>
			</div>
		</section>

		<section id="quote-tool" class="section-padding">
			<div class="container">
				<h2>Build Your Custom Package</h2>
				<p style="text-align: center; margin-bottom: 3rem; color: var(--text-gray);">Select a popular bundle to start, or pick individual services below.</p>
				
				<div class="grid" style="margin-bottom: 3rem;">
					<div class="card bundle-card" onclick="selectBundle(['photos'])">
						<h3>Starter</h3>
						<span class="bundle-price">$150</span>
						<ul class="bundle-features">
							<li>✅ Interior & Exterior Photos</li>
							<li>❌ Drone Aerials</li>
							<li>❌ 3D Virtual Tour</li>
						</ul>
						<button class="btn" style="width: 100%; font-size: 0.9rem;">Select Starter</button>
					</div>
					<div class="card bundle-card" style="border-color: var(--accent); transform: scale(1.05);" onclick="selectBundle(['photos', 'drone'])">
						<h3 style="color: var(--accent);">Most Popular</h3>
						<span class="bundle-price">$225</span>
						<ul class="bundle-features">
							<li>✅ Interior & Exterior Photos</li>
							<li>✅ Drone Aerials</li>
							<li>❌ 3D Virtual Tour</li>
						</ul>
						<button class="btn" style="width: 100%; font-size: 0.9rem;">Select Pro</button>
					</div>
					<div class="card bundle-card" onclick="selectBundle(['photos', 'drone', 'tour'])">
						<h3>Premium</h3>
						<span class="bundle-price">$350</span>
						<ul class="bundle-features">
							<li>✅ Interior & Exterior Photos</li>
							<li>✅ Drone Aerials</li>
							<li>✅ 3D Virtual Tour</li>
						</ul>
						<button class="btn" style="width: 100%; font-size: 0.9rem;">Select Premium</button>
					</div>
				</div>

				<div class="card">
					<h3 style="margin-bottom: 1.5rem;">Pick & Choose Services</h3>
					<div class="ala-carte-list">
						<label class="checkbox-label">
							<div>
								<input type="checkbox" id="photos" value="150" onchange="calculateTotal()">
								<strong>Interior & Exterior Photos</strong>
							</div>
							<span>$150</span>
						</label>
						<label class="checkbox-label">
							<div>
								<input type="checkbox" id="drone" value="75" onchange="calculateTotal()">
								<strong>Drone Aerials (Add-on)</strong>
							</div>
							<span>+$75</span>
						</label>
						<label class="checkbox-label">
							<div>
								<input type="checkbox" id="tour" value="125" onchange="calculateTotal()">
								<strong>3D Virtual Tour (Add-on)</strong>
							</div>
							<span>+$125</span>
						</label>
						<label class="checkbox-label">
							<div>
								<input type="checkbox" id="twilight" value="50" onchange="calculateTotal()">
								<strong>Virtual Twilight (Add-on)</strong>
							</div>
							<span>+$50</span>
						</label>
					</div>
				</div>

				<div class="quote-total-box